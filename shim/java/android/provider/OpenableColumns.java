package android.provider;

/**
 * A2OH shim — android.provider.OpenableColumns
 *
 * Marker interface providing standard column constants for openable URIs
 * returned by content providers that support the "openable" category.
 */
public interface OpenableColumns {
    String DISPLAY_NAME = "_display_name";
    String SIZE = "_size";
}
