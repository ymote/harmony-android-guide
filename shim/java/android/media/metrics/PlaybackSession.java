package android.media.metrics;

/**
 * Android-compatible PlaybackSession stub (API 31+).
 * A2OH shim layer — OpenHarmony migration.
 */
public class PlaybackSession implements AutoCloseable {
    PlaybackSession() {}

    @Override
    public void close() {}
}
