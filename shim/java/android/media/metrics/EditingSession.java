package android.media.metrics;

/**
 * Android-compatible EditingSession stub (API 31+).
 * A2OH shim layer — OpenHarmony migration.
 */
public class EditingSession implements AutoCloseable {
    EditingSession() {}

    @Override
    public void close() {}
}
