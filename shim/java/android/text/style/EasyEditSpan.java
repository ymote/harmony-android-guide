package android.text.style;
import android.app.PendingIntent;
import android.app.PendingIntent;

import android.app.PendingIntent;

/**
 * A2OH shim — Android-compatible stub.
 *
 * Span that marks a region as easy-to-edit. The platform may offer UI
 * affordances such as a pop-up to delete or modify the spanned text.
 */
public class EasyEditSpan {

    /** Creates an EasyEditSpan. */
    public EasyEditSpan() {
    }

    /**
     * Creates an EasyEditSpan that will fire the given {@link PendingIntent}
     * when the enclosing text is modified.
     *
     * @param pendingIntent the PendingIntent to fire
     */
    public EasyEditSpan(PendingIntent pendingIntent) {
    }
}
